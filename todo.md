##next steps
#06.12.2025
- solve the logic, still is inconsistent between rules & requirements
- solve the security flaws of the pentest founded today in the test environment layer (localhost only & admin level credentialized vs / external user)
    - zap for endpoints
    - sast to do
    - VA testing
    ** once deployed in prod, to redo the pentest in iso-prod, credentialized and non credentialised
- eliminate ai entions to avoid ai mentions(?)
