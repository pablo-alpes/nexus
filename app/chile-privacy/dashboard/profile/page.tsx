'use client';

/**
 * Chilean Privacy Dashboard - Profile
 * Route: /chile-privacy/dashboard/profile
 * Reuses the same profile UI; pathname makes isChileanPrivacy true
 */

import ProfilePage from '@/app/dashboard/profile/page';

export default function ChileanPrivacyProfilePage() {
  return <ProfilePage />;
}
